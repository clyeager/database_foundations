1.SQL is a declarative programming language, and is also a special purpose language since it
is only used to interact with relational databases.

2.The three sublanguages are Data Definition Language(DDL), Data Manipulation Language(DML), and Data Control Language(DCL).

3.'canoe' 'a long road' 'weren''t' '"No way!"'

4.||

5.lower
SELECT lower('MAKE tHis LowerCasE')

6.t f

7.SELECT trunc(4 * pi() * 26.3 ^ 2);